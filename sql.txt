select * from student;

show databases;
use collage;
create table IF NOT EXISTS students
(
    id int PRIMARY KEY,
    name VARCHAR(20),
    age int NOT NULL,
    mark int 
);

insert into students(id , name , age , mark)
VALUES
(101 , "Soumya", 22 , 89),
(103, "Satya", 24, 90),
(102, "Anuj",23,99),
(104,"Rohan",21,75),
(105,"Rudra",21,78),
(106,"Amit",22,70),
(107, "Chiku",23,69),
(108, "Soam",24,85),
(109, "Subham",23,86),
(110 , "Sobhan",21,65);

SELECT * from students;
select name, mark from students;
select DISTINCT age from students;

-- change column name --
alter table students
change column adress address varchar (20);

-- where clause --

select * from students where mark < 80;
show TABLES;

rename student to records;

DESCRIBE students;

select * from students;
alter table students
add column adress varchar (10)
after mark;

SET SQL_SAFE_UPDATES = 1;


alter table students drop column adress;

UPDATE students
SET adress = 'Mumbai'
WHERE id BETWEEN 109 AND 110;


select * from students
where mark >= 90;

SELECT * from students
where mark > 80 and adress = 'Pune';

SELECT * from students 
ORDER BY mark desc limit 3;

---- aggr fun ----
select max(mark) as Max_marrk
from students;

select avg(mark) as average_mark
from students;

select count(id)
from students;

-- group by --
select address , name , count(id) as student_count
from students 
group by address , name;

select address , count(id) as Student_count , AVG(mark) as avg_markes
from students
GROUP BY address;

select address , AVG(mark) as avg_mark
from students
GROUP BY address
ORDER BY avg_mark DESC;


-- task --
CREATE TABLE payments (
    customer_id INT PRIMARY KEY,
    customer VARCHAR(100),
    mode VARCHAR(50),
    city VARCHAR(100)
);

INSERT INTO payments (customer_id, customer, mode, city) VALUES
(101, 'Olivia Barrett', 'Netbanking', 'Portland'),
(102, 'Ethan Sinclair', 'Credit Card', 'Miami'),
(103, 'Maya Hernandez', 'Credit Card', 'Seattle'),
(104, 'Liam Donovan', 'Netbanking', 'Denver'),
(105, 'Sophia Nguyen', 'Credit Card', 'New Orleans'),
(106, 'Caleb Foster', 'Debit Card', 'Minneapolis'),
(107, 'Ava Patel', 'Debit Card', 'Phoenix'),
(108, 'Lucas Carter', 'Netbanking', 'Boston'),
(109, 'Isabella Martinez', 'Netbanking', 'Nashville'),
(110, 'Jackson Brooks', 'Credit Card', 'Boston');

-- total payment according to each payment method

select mode , COUNT(customer) as customer_no 
from payments
GROUP BY mode;

select address , count(id) as num_student
from students
GROUP BY address
HAVING max(mark) > 90;

select address , count(mark)
from students
GROUP BY address;

-- general order 

SELECT column(s)
FROM table_name
WHERE condition
GROUP BY column(s)
HAVING condition
ORDER BY column(s) ASC;
--

select address , mark ,age, COUNT(id) as numberofstudent
from students
where age BETWEEN 22 and 23
group by address
having mark > 90
ORDER BY address desc;




select * from students;
show DATABASES;
use demo;

show tables;

select * from students;

alter table students
add COLUMN email varchar(20);

alter table students
drop column email;

SET SQL_SAFE_UPDATES = 0;

show DATABASES;


show tables;

select * from students;

---------------------------------------rev---------------------------------------

select name , age 
from students
where address = 'pune';

ALTER TABLE students RENAME COLUMN address TO city;

select name,age,mark
from students
where city = "delhi"
ORDER BY mark desc;

select * from students 
where mark >= 80;

select * from students 
where age BETWEEN 20 and 22;

select * from students 
where city = 'pune' or city = 'delhi';

------agg funs---

select name , max(mark) as maxMark
from students 
where city = 'pune' and age BETWEEN 20 and 25
GROUP BY name 
ORDER BY maxMark desc;

select city , count(name) 
from students 
GROUP BY city;

select city , CAST(avg(age) AS SIGNED) as AvgAge 
from students 
GROUP BY city;


select city , avg(mark) as avgMark
from students 
GROUP BY city
having avgMark > 80;


select city , count(name) SC
from students
group by city
having SC > 2;

select * from students
where mark = (
    select max(mark)
    from students
);

select * from students
where mark > (
    SELECT avg(mark)
    from students
);


-----------------------------------revjr-----------------------------------------------


show databases;
use collage;
show tables;
SELECT * from students;

---CRUD------
#create

create table course (
    course_id int primary key,
    course_name varchar(50),
    instructor varchar(30),
    duration_months int,
    fees decimal(10,2)
);


insert into course (course_id , course_name,instructor,duration_months,fees)
values
(201, 'Python', 'Alice', 3, 15000),
(202, 'Java', 'Bob', 4, 18000),
(203, 'SQL', 'Charlie', 2, 12000),
(204, 'AI Basics', 'David', 5, 25000),
(205, 'Web Development', 'Emma', 4, 20000);

select * from students;

CREATE TABLE enrollments (
    enrollment_id INT PRIMARY KEY,
    student_id INT,
    course_id INT,
    enrollment_date DATE,

    FOREIGN KEY (student_id) REFERENCES students(id),
    FOREIGN KEY (course_id) REFERENCES course(course_id)
);

INSERT INTO enrollments VALUES
(1,101,201,'2025-01-10'),
(2,101,203,'2025-01-12'),
(3,102,202,'2025-01-15'),
(4,103,201,'2025-01-18'),
(5,104,204,'2025-01-20'),
(6,105,203,'2025-01-22'),
(7,106,205,'2025-01-24'),
(8,107,201,'2025-01-26'),
(9,108,202,'2025-01-28'),
(10,109,204,'2025-02-01'),
(11,110,205,'2025-02-05');

select * from enrollments;

show tables;

select * from payments;

UPDATE payments
set mode = "Debit Card"
where customer_id = 101;

update payments
set customer = 'Lucas jain',
    city = 'New York'
where customer_id = 108;



multi rows and cols u
update payments
set 
    city = case customer_id
    when 101 THEN 'Miami'
    when 102 then 'Portland'
    when 103 then 'Denver'
    when 104 then 'Seattle'
end,

    mode = case customer_id
    when 101 then 'Netbanking'
END
where customer_id in (101,102,103,104);

show tables;

select * from users;

alter table users
add column city varchar(40); 
insert into users (city) values ('pune') ; ----will never work as there are 2 more filds are also present 
alter table users 
rename column name to user_name;



#ALTER TABLE [table_name] 
#    ├── ADD      [column_name] [datatype]
#    ├── DROP     COLUMN [column_name]
#    ├── RENAME   COLUMN [old_name] TO [new_name]
#    └── MODIFY   COLUMN [column_name] [new_datatype]



update users
set 
    city = case id
    when 1 then 'Pune'
END 
where id = 1;

alter table users 
add column age int after user_name;

update users 
set 
    age = case id
    when 1 then 23
END
where id = 1;


alter table users 
MODIFY column email varchar(60) not null;

alter table users 
add constraint uq_email
unique (email);
desc users;

select * from users;

alter table users 
drop column age;


---  --where clouse ------

select * from students;

select * from students 
where city = 'pune';

select * from students 
where mark >= 90;

select * from students 
where mark between 80 and 90;

select * from students
where age between 19 and 22;

select * from students;

select name ,mark from students 
where city = 'mumbai';

select name , mark from students
ORDER BY mark desc
LIMIT 3
OFFSET 1; ----2nd last

select name , mark from students
where mark>=90 and city = 'pune';

select * from students
where (city = 'delhi' or city = 'mumbai' )and mark > 80;

select * from students 
where name like 'S%';

SELECT *
FROM students
ORDER BY mark DESC;

select count(*) as total
from students;
select sum(mark) as total_mark 
from students;

select avg(mark) as avg_mark
from students;

SELECT
COUNT(*) AS TotalStudents,
AVG(mark) AS AverageMarks,
MAX(mark) AS HighestMarks,
MIN(mark) AS LowestMarks,
SUM(mark) AS TotalMarks
FROM students;

select city , count(name) as total
from students
where mark > 70
GROUP BY city
ORDER BY city asc;
